# Generate sequence of even integers greater than 50
even_numbers <- seq(from = 52, by = 2, length.out = 15)

# Create 5x3 two-dimensional array
my_array <- array(even_numbers, dim = c(5, 3))

# Display the array
print(my_array)
